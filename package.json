{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "predeploy": "yarn generate",
    "deploy": "echo labfero.com > .output/public/CNAME && echo > .output/public/.nojenkyll && node deploy.js"
  },
  "devDependencies": {
    "@nuxtjs/google-fonts": "^2.0.0",
    "@nuxtjs/tailwindcss": "^6.4.1",
    "gh-pages": "^5.0.0",
    "nuxt": "^3.2.3"
  }
}
